function fakeLogout(){alert("Demo only — logout is not connected to a real authentication system.");}
function submitFakeRequest(e){e.preventDefault();alert("Demo request submitted. No backend or real authentication is connected.");return false;}
